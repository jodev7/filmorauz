# HTTP / scraping
requests>=2.31.0

# Host metrics for /healthz (admin VPS status panel)
psutil>=5.9.0

beautifulsoup4>=4.12.0
lxml>=4.9.0

# Environment
python-dotenv>=1.0.0

# Instagram
instagrapi>=2.0.0

# YouTube download (manual import)
yt-dlp>=2024.1.0

# YouTube (OAuth2 + Data API)
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.2.0

# N_m3u8DL-RE binary wrapper
ddownloader>=0.1.0

# Playwright (uzmovi.py media extraction fallback)
playwright>=1.40.0

# AI clip analysis (optional — used in /clip/analyze endpoint)
# faster-whisper (CTranslate2) replaces openai-whisper: better Uzbek accuracy
# with the `medium` model, fast enough on CPU (int8) to transcribe whole films.
faster-whisper>=1.0.0
opencv-python-headless>=4.8.0

# AI viral clip selection (used in /clip/gemini-analyze endpoint)
google-genai>=1.0.0
